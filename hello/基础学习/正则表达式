正则表达式：字符串检索引擎，起源于unix

1. unix下的正则：awk  grep egrep
2. 正则的基本用法：

[0-9] 或  /d 全部数字
\w 单词类字符（a-z  A-Z 0-9 _）
\W 非单词类字符

{2}  {n} 前面的表达式匹配n次
{0,2} {m,n} 前面的表达式匹配m到n次

+ 前面的表达式，出现1到无限次    最少出现一次
? 前面的表达式，出现0到1次       最多出现一次
* 前面的表达式，出现0到无限次    是否出现无所谓

^n  以n开头

3. Python里面的正则
import re
print re.match(r"\d","123")   #匹配不到返回None

